This is what's going on, you will have to do this.

<%= form_for(@story) do |f| %>
Who is writing this story?
<%= f.collection_select :writer_ids, Writer.all, :id, :pen_name %>
What is the title of your story?
<%= f.label :title %>
<%= f.text_field :title %>
<%= f.submit %>
<% end %>
